import 'package:flutter/material.dart';

class WatchedHistoryScreen extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Back'),
      ),
      body: Center(
        child: Text('View all your watched movies here'),
        ),
      );
  }
}
